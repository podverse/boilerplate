# Web env for Docker (infra/config/local/web.env) – minimal for main container.
# Full list for the runtime-config sidecar: apps/web/sidecar/.env.example

# For Docker: sidecar service name and port. For local dev: http://localhost:4001
RUNTIME_CONFIG_URL="http://boilerplate_local_web_sidecar:4101"
