#####
##### Boilerplate Valkey – used by API for message storage (plan 22).
##### Copy to infra/config/local/valkey.env or merge into api.env.
#####

VALKEY_HOST="valkey"
VALKEY_PORT="6379"
# From host CLI: use port 6380 (host mapping; avoids conflict with podverse 6379)
# VALKEY_PASSWORD=""
