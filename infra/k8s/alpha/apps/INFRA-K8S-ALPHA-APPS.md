# Alpha child applications placeholder

Add ArgoCD child `Application` manifests here when alpha rollout is activated.

For now, this remains empty by design while local k3d + ArgoCD is implemented first.
