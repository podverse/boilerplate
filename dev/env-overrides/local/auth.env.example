# Auth mode. Sensible default applied to API and management-api by make local_env_setup.
# admin_only_username | admin_only_email | user_signup_email.
# Prepared by local_env_prepare; edit in ~/.config/boilerplate/local-env-overrides/ after link.

AUTH_MODE="admin_only_username"
