# Locale defaults. One place for all apps; setup applies to API, web, management-api, management-web (including sidecars). Do not set DEFAULT_LOCALE or SUPPORTED_LOCALES in other override files.
# Prepared by local_env_prepare; edit in ~/.config/boilerplate/local-env-overrides/ after link.

DEFAULT_LOCALE="en-US"
SUPPORTED_LOCALES="all-available"
