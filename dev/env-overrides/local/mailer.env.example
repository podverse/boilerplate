# Mailer (SMTP). No defaults — bring your own. Applied to API by make local_env_setup when present.
# Tests/CI use mailpit. Prepared by local_env_prepare; edit in ~/.config/boilerplate/local-env-overrides/ after link.

SMTP_HOST=
SMTP_PORT=
MAIL_FROM=
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=
