/**
 * Raw token for reset-password E2E (short to avoid URL truncation). Must match the token seeded in tools/web/seed-e2e.mjs
 * (hash stored in verification_token for E2E user e2e@example.com).
 */
export const E2E_RESET_PASSWORD_TOKEN_RAW = 'e2e0' + '0'.repeat(28);
