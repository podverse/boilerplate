# COPY-PASTA prompts

Use these prompts phase-by-phase. Do not start a later phase until prior phase is complete.

## Phase 1 prompt (single agent)

Implement plan `01-inheritance-governance-foundation.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Apply root-scoped governance resolution and descendant name-only mutation constraints.
Return: files changed, key logic decisions, and verification results.

## Phase 2 prompt (single agent)

Implement plan `02-recursive-routing-model.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Replace topic route helper semantics with ancestry-based private/public bucket path helpers.
Return: helper API, migration notes for callers, and verification results.

## Phase 3 parallel prompts (run together)

### Agent A
Implement `03-web-hard-cut-routes-and-pages.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Hard-cut topic routes in web app and migrate to recursive bucket pages/links.

### Agent B
Implement `04-management-web-hard-cut.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Hard-cut topic routes in management-web and migrate to recursive bucket pages/links.

## Phase 4 prompt (single agent)

Implement `05-api-contracts-and-guards.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Rewrite contracts and enforce descendant guardrails.

## Phase 5 prompt (single agent)

Implement `06-orm-sql-and-data-model.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Converge ORM/SQL naming and model behavior with bucket-only semantics.

## Phase 6 prompt (single agent)

Implement `07-client-helpers-and-types.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Update helper packages and all call sites to bucket-chain semantics.

## Phase 7 parallel prompts (run together)

### Agent A
Implement `08-i18n-docs-terminology.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Remove topic terminology and add inherited-governance copy.

### Agent B
Implement `09-openapi-tests-cutover.md` in `/Users/mitcheldowney/repos/pv/boilerplate`.
Update OpenAPI and tests for hard cut and recursive routes.
