# Generate-data tool – Execution order

## Plan file location

All plans: `.llm/plans/completed/boilerplate/generate-data/`

## Phase 1 (single phase)

1. **01-generate-data-package.md** – Implement package, docs, and skill.

No dependencies on other plan sets. Run from repo root after `npm run build:packages`.
