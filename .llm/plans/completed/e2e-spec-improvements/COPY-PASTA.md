# COPY-PASTA – Implement E2E spec improvements one by one

Submit **one prompt at a time** (copy the entire block). Complete every step in the plan for that spec, run the targeted spec, then set the plan to **Status: Completed** with a **Done** line describing what was implemented. Do not skip plan steps.

Order follows `00-EXECUTION-ORDER.md` (Phase 1 → 2 → 3 → 4).

---

## Phase 1 – Web

### 1. web-bucket-admin-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-admin-edit.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-admin-edit.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-admin-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 2. web-home

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-home.md, implement every step in "Steps to implement" for apps/web/e2e/home.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/home.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 3. web-login

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-login.md, implement every step in "Steps to implement" for apps/web/e2e/login.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/login.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 4. web-dashboard

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-dashboard.md, implement every step in "Steps to implement" for apps/web/e2e/dashboard.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/dashboard.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

---

## Phase 1 – Management-web

### 5. mgmt-bucket-admin-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-admin-edit.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-admin-edit.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-admin-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 6. mgmt-home

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-home.md, implement every step in "Steps to implement" for apps/management-web/e2e/home.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/home.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 7. mgmt-login

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-login.md, implement every step in "Steps to implement" for apps/management-web/e2e/login.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/login.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 8. mgmt-dashboard

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-dashboard.md, implement every step in "Steps to implement" for apps/management-web/e2e/dashboard.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/dashboard.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

---

## Phase 2 – Web permission-gated

### 9. web-bucket-role-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-role-edit.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-role-edit.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-role-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 10. web-bucket-role-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-role-new.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-role-new.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-role-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 11. web-bucket-settings

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-settings.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-settings.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-settings.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 12. web-bucket-message-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-message-edit.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-message-edit.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-message-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 13. web-bucket-messages

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-messages.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-messages.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-messages.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 14. web-bucket-detail

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-detail.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-detail.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-detail.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 15. web-bucket-child-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-child-new.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-child-new.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-child-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 16. web-bucket-nested-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-bucket-nested-new.md, implement every step in "Steps to implement" for apps/web/e2e/bucket-nested-new.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/bucket-nested-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 17. web-buckets-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-buckets-new.md, implement every step in "Steps to implement" for apps/web/e2e/buckets-new.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/buckets-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 18. web-buckets

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-buckets.md, implement every step in "Steps to implement" for apps/web/e2e/buckets.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/buckets.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 19. web-short-bucket

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-short-bucket.md, implement every step in "Steps to implement" for apps/web/e2e/short-bucket.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/short-bucket.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 20. web-send-message

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-send-message.md, implement every step in "Steps to implement" for apps/web/e2e/send-message.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/send-message.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 21. web-invite

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-invite.md, implement every step in "Steps to implement" for apps/web/e2e/invite.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/invite.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 22. web-profile

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-profile.md, implement every step in "Steps to implement" for apps/web/e2e/profile.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/profile.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 23. web-settings

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-settings.md, implement every step in "Steps to implement" for apps/web/e2e/settings.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/settings.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

---

## Phase 3 – Management-web permission-gated

### 24. mgmt-admin-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-admin-edit.md, implement every step in "Steps to implement" for apps/management-web/e2e/admin-edit.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/admin-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 25. mgmt-admin-detail

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-admin-detail.md, implement every step in "Steps to implement" for apps/management-web/e2e/admin-detail.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/admin-detail.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 26. mgmt-admin-role-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-admin-role-new.md, implement every step in "Steps to implement" for apps/management-web/e2e/admin-role-new.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/admin-role-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 27. mgmt-admins-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-admins-new.md, implement every step in "Steps to implement" for apps/management-web/e2e/admins-new.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/admins-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 28. mgmt-admins

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-admins.md, implement every step in "Steps to implement" for apps/management-web/e2e/admins.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/admins.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 29. mgmt-user-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-user-edit.md, implement every step in "Steps to implement" for apps/management-web/e2e/user-edit.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/user-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 30. mgmt-user-detail

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-user-detail.md, implement every step in "Steps to implement" for apps/management-web/e2e/user-detail.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/user-detail.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 31. mgmt-users-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-users-new.md, implement every step in "Steps to implement" for apps/management-web/e2e/users-new.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/users-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 32. mgmt-users

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-users.md, implement every step in "Steps to implement" for apps/management-web/e2e/users.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/users.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 33. mgmt-bucket-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-edit.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-edit.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 34. mgmt-bucket-role-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-role-edit.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-role-edit.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-role-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 35. mgmt-bucket-role-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-role-new.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-role-new.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-role-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 36. mgmt-bucket-settings

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-settings.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-settings.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-settings.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 37. mgmt-bucket-message-edit

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-message-edit.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-message-edit.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-message-edit.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 38. mgmt-bucket-messages

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-messages.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-messages.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-messages.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 39. mgmt-bucket-detail

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-detail.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-detail.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-detail.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 40. mgmt-bucket-child-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-bucket-child-new.md, implement every step in "Steps to implement" for apps/management-web/e2e/bucket-child-new.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/bucket-child-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 41. mgmt-buckets-new

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-buckets-new.md, implement every step in "Steps to implement" for apps/management-web/e2e/buckets-new.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/buckets-new.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 42. mgmt-buckets

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-buckets.md, implement every step in "Steps to implement" for apps/management-web/e2e/buckets.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/buckets.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 43. mgmt-profile

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-profile.md, implement every step in "Steps to implement" for apps/management-web/e2e/profile.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/profile.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 44. mgmt-settings

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-settings.md, implement every step in "Steps to implement" for apps/management-web/e2e/settings.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/settings.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 45. mgmt-events

```
Using the plan in .llm/plans/active/e2e-spec-improvements/mgmt-events.md, implement every step in "Steps to implement" for apps/management-web/e2e/events.spec.ts. Do not skip any steps. Run `make e2e_test_management_web_report_spec SPEC=e2e/events.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

---

## Phase 4 – Web auth-only

### 46. web-signup

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-signup.md, implement every step in "Steps to implement" for apps/web/e2e/signup.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/signup.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 47. web-forgot-password

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-forgot-password.md, implement every step in "Steps to implement" for apps/web/e2e/forgot-password.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/forgot-password.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```

### 48. web-reset-password

```
Using the plan in .llm/plans/active/e2e-spec-improvements/web-reset-password.md, implement every step in "Steps to implement" for apps/web/e2e/reset-password.spec.ts. Do not skip any steps. Run `make e2e_test_web_report_spec SPEC=e2e/reset-password.spec.ts`. When done, set the plan to Status: Completed and update the Done line with what was implemented.
```
